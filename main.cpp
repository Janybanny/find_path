#include <iostream>
#include <string>
#include <map>
#include "station.h"

int main(int argc, char **argv) {
    std::map<std::string, Station*> allstations; // Alle Stationen
    std::map<std::string, Station*> faststations; // Nur Umsteigestationen
    return 0;
}
